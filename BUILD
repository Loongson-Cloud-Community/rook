因为 ceph/go-ceph 子项目存在条件编译

```
//go:build ceph_preview
 // +build ceph_preview

 package admin

 import (
     "context" 
     "encoding/json" 
     "fmt" 
     "net/http" 
 )

```

go build --tags="ceph_preview" ./cmd/rook/
